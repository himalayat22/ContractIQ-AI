/** BullMQ job names within the ingestion queue. */
export const INGESTION_JOB_NAMES = {
  EXTRACT: 'extract',
};

/** BullMQ job names within the AI analyze queue. */
export const AI_ANALYZE_JOB_NAMES = {
  ANALYZE: 'analyze',
};

/** BullMQ job names within the analysis complete queue. */
export const ANALYSIS_COMPLETE_JOB_NAMES = {
  COMPLETE: 'complete',
};

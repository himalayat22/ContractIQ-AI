# Docker Compose overrides (optional — copy to repo root as .env or export before `docker compose up`)
# Hostnames below are for services running on your machine, not inside the Compose network.

API_GATEWAY_PORT=4000
IDENTITY_SERVICE_PORT=4001
CONTRACT_SERVICE_PORT=4002
MONGODB_PORT=27017
REDIS_PORT=6379

WEB_URL=http://localhost:5173
JWT_SECRET=change-me-use-openssl-rand-hex-64

MONGODB_DB_IDENTITY=contractiq_identity
MONGODB_DB_CONTRACT=contractiq_contract
